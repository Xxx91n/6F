# 45: 演示入口——fixture 生成器＋三场景 definitions＋golden＋demo --scenario 命令＋CASRAI 式披露块

**A-xxx covered:** A-050
**Spec ref:** [spec.md](spec.md) §R6-02（spec-phase-tasks.md R6-02 行）

**What to build:**
内置合成 fixture 演示入口（D-038）：① `engine/fixtures/definitions/*.json` 三场景定义（happy-path／degraded-supply／degraded-incomplete，名逐字）＋ `fixtures/golden/` 预期报告形态 ＋ 生成器随 tgz 分发；生成器输出确定性 git 仓（merge／多分支／tag，FerrLabs 模式），临时目录生成跑完即弃；② `demo [--scenario]` CLI 命令（默认 happy-path）；③ CASRAI 式机器可读披露块——`fixture: synthetic (generated by <ver>)`｜`not an audit of any real repository`｜`capability 1 of 5 · preview`｜`supply-chain: ⚠ unverified`，复用 #38 preview_disclosure 同一字段契约（D-037② 统一契约面，禁两处手抄）；④ demo 内部走同一 Repo Intake 本地路径（D-013 classifyRepoInput local leg，不设新输入面）。合成数据不冒充真实审计结论（披露块=硬契约非脚注）；失败路径为一等演示场景可确定性触发（degraded-supply=D-034④ 供应链未接演示化）。

**Blocked by:**
None（DoR 闭合；#43 golden CI 消费本票 definitions，反向依赖不阻塞本票）

**Status:** done（2026-09-16 实现完成——生成器+三场景 definitions+golden+demo 命令+披露块落盘，45-check PASS）

- [x] fixture 生成器：合成审计数据生成器（确定性 git 仓输出，显式 synthetic 标记）→ `engine/src/demo/fixture-generator.ts`（pin author/committer/date → 逐字节确定性 SHA）
- [x] fixtures/definitions 三场景定义文件落盘 → `engine/fixtures/definitions/{happy-path,degraded-supply,degraded-incomplete}.json`
- [x] fixtures/golden 三场景预期报告形态 → `engine/fixtures/golden/{scenario}.{md,json}` ×3（`engine/scripts/gen-demo-golden.mjs` 同管线重渲染）
- [x] demo --scenario 命令（临时目录生成、跑完即弃、三场景可跑）→ `engine/src/demo/demo.ts` + `engine/src/cli.ts` `demo` 子命令
- [x] CASRAI 式披露块复用 preview_disclosure 同一字段契约＋synthetic fixture 印记 → demo.ts `DEMO_DISCLOSURE()`（四印记逐字：fixture: synthetic (generated by fixture-generator@1.0.0)／not an audit of any real repository／capability 1 of 5 · preview／supply-chain: ⚠ unverified）
- [x] demo 走同一 Repo Intake 本地路径（D-013）→ `repoAdd`/`classifyRepoInput` local leg（`test/demo.test.mjs` L 组断言）
- [x] 守卫 reports/45-check.mjs PASS → 断言面见 45-report §6
