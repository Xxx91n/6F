# closeout-2026-09-15 — 轮 6（R4 量测效度先行）收口交接

## 本轮结算

R4 全链六票闭环：#26（量测审计）→（#27 判据v2 ∥ #28 治理卫生）→ #29（C层 disposition）→ #30（2a 冻结校准）→ #31（2b CodeLore 探针）。
账本 A-031~A-036 全 done；六守卫 18+15+113+13+9+14 = **182 断言全 PASS**；engine 侧 `npm test` 26/26、`npm run package` tgz 31 文件、`cli selftest` ok=true。

## 交接给下一轮（轮 7 = 阶段 3 铺开）

### 已拍未做（frontier 依序）
1. **plugin.json schema 修复票**（#31 P1 预核对发现）：engine/plugin.json 不合 Agent Plugins 1.0.0（缺 `$schema` const / `schemaVersion`/`skills`/`mcp` 越界属性 / `extensions` 数组 vs 对象表）——须按 1.0.0 重写并跑 ajv validate；并入分发收尾或独立票。
2. **CodeLore 契约面扩开**：56 analysis 仅契约化 explain/summary/version 三面；阶段 3 按需补（hotspots/bus-factor 等与 S3/S4/S5 矩阵行对应）。
3. **任务 7 多写者域 self-probe**：阶段 3 多采集器并行写 fact 表时实测封口。
4. **Scorecard / repomix 探针**：阶段 3 窗口（D-024：两上游字段形状须实跑固化）。
5. **Micro-A/B 真实仓试点**：任务 15 边界草案待真实行级证据。

### 关键在库资产
- `reports/26-truth-table.json` = golden set（后续 detector 版本回归基线）
- `reports/27-dual-readings.*` = 勘误式双读数发布模板
- `reports/30-desk-calibration.json` = 2a 校准清单（13 项 + 置信域）
- `engine/src/upstream/codelore.ts` = 首个上游适配器（binary-discovery + pin 模式可复用到 Scorecard/repomix）
- `engine/test/fixtures/codelore/` = golden cassette 先例

### 纪律延续
- 栈顶 = 31-codelore-probe(oyp)；全栈未 push（发布属用户闸门）。
- reopen 惯例：账本 C 裁定节 `### disposition 补记（日期，票号，CAPA reopen）` 追加模式已立例（#29）。
- 检测器版本是读数一部分：v1/v2 并存于 facts/detector 列，发布读数必带版本戳。
