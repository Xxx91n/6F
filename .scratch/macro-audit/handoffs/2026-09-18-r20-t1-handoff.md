# Handoff — 轮 20 T1 / #59 kernel 自包含分发修复（2026-09-18 · commit lvr · 分支 r20-59-kernel-selfcontained）

## Goal

任务书 `D:\Aworker\6F\.scratch\macro-audit\handoffs\next-round.md` 轮 20 常驻任务书 T0→T9 依序执行。本会话完成 **T1 / BACKLOG #59（P0）**：kernel 自包含分发修复（D-067/D-038/D-037⑥）——T6 实证外部用户装出死 MCP server 的根因根治。

## Accomplished（本轮已落地，勿重做）

1. `.mcp.json` 自包含：`command:"node"` + `args:["${CLAUDE_PLUGIN_ROOT}/dist/cli.js","mcp"]`（变量放 args 不放 command；gen-manifests.mjs 单源生成；`mcp.json` 兄弟文件保留 bare `macro-audit` 为 npm 消费者面不动）。
2. `engine/dist/` 40 件随源入库：`engine/.gitignore` 放开 `dist/`；dist/cli.js=esbuild 0.28.2 单文件 bundle（153.6KB）+tsc 模块树；`scripts/build-bundle.mjs` 新增（`packages:external`，shebang 沿源）；`package.json` build=tsc+bundle；esbuild 入 devDependencies 精确值+package-lock+upstream-lock（active/exact-version）。
3. duckdb 懒加载降级：`engine/src/fact/store.ts` dynamic import——clone 无 node_modules 时握手/selftest/repo-add/demo --list 零依赖可用，facts/audit/demo 报 `DUCKDB-UNAVAILABLE` 结构化错（仿真插件目录实测）；`engine/src/demo/demo.ts` fixtures 锚改 `metaPath()`（bundle/tsc 两形态同构）。
4. CI rebuild-diff：`.github/workflows/engine-ci.yml` build 后 `test -z "$(git status --porcelain -- dist/)"`（shell:bash——porcelain 捕未跟踪新文件）。
5. 守卫换约：34-check G9（新契约）+G12（command 禁裸名）+G13（dist/cli.js 存在且 bundle 形）+G14（.gitignore 放开）→PASS 14/14；41b-check A4 换约→PASS 30/30。
6. 文档同票：engine/README.md §插件安装全节；SKILL.md 运行前提；根 README 状态注（过渡句式）。
7. **真机验收（等价环）**：`claude --plugin-dir D:/Aworker/6F/engine mcp list` → `plugin:6f:macro-audit-kernel: node D:/Aworker/6F/engine/dist/cli.js mcp - ✔ Connected`（claude 2.1.251/Windows）——`${CLAUDE_PLUGIN_ROOT}` exec-form 展开实证免疫 #43380/#65579 hook 面 bug 链；`claude plugin validate engine --strict` 清零（D-066 advisory 顺带实证）。
8. 版本控制：分支 `r20-59-kernel-selfcontained` commit `lvr`（55 件，含 dist 40 新增）；账本 R11/A-069 落账＋BACKLOG #59 ✅＋WORKFLOW lessons 行＋本报告 `D:\Aworker\6F\.scratch\macro-audit\reports\2026-09-18-report.md`。

## Discoveries / 如实披露的缺口

- **干净 clone 仍无 duckdb**：bundle 面=握手+selftest+repo-add+demo --list 零依赖；facts/audit/demo 跑批需插件目录 `npm install --omit=dev`。候选=vendored bindings（仓重+治理）／SessionStart bootstrap（bug 链未免疫）／duckdb-wasm（API 异构）——另立票待裁。
- **字面验收残余**：D-067⑧「/plugin install→/mcp」需新 commit push 后市场缓存更新——push 停用户闸门；plugin-dir 环已覆盖宿主拉起链同一路径。
- **41a-check 陈旧 FAIL 4/39**（非本票引入）：C10 与 41b-C1 互斥／D6 D7 CHANGELOG 未随 A-068/D-070 进账（现 D6 want 又漂至 A-069）／F4 next-round 已换代——R19 收口件（xql）漂移，冻结快照按 R14 lesson 不复写，移交 T8。
- 守卫全绿：33/34/41b/42/44/46/52a/53/54/55 + npm test exit 0（15 套件）+ npm pack 71 件 167KB。

## Remaining（任务书 T2~T9 未动）

| T | 内容 | 依赖/注意 |
|---|---|---|
| T2 | #58 manifest 契约守卫链扩（D-066） | 与 #59 同触 34/41b-check——我已在 G9/G12~G14/A4 落定新契约断言，T2 在其上扩 skills^\./＋字段白名单＋validate advisory＋schemastore 快照 |
| T3 | #61 cue 表分层＋r18 顺带清（D-069/D-070） | 判定 commit 与 [cleanup] commit 必须分离；禁参照 52a 标签调参 |
| T4 | #60 骨架 breaking-diff/升版机检（D-068） | committed baseline JSON 非 git diff（R19-Q3） |
| T5 | #62 doctor 探测（D-059③） | runtime-doctor-trigger 已 triggered-bound |
| T6 | #52b 叙事评测面 | 触发器=host-narrative-corpus 出现，未出现则记注 |
| T7 | #41b 残余 listing 核对 | marketplace.json/checklist 同步面 |
| T8 | registry/值守复核 | 含我移交的 41a 陈旧面＋duckdb-binary-watch／bundle-retirement-trigger 关联项复核 |
| T9 | D-025 双读数纪律呈报 | 核验非重裁 |

## Instructions / 环境

- 分支 `r20-59-kernel-selfcontained`（lvr）未 push——用户闸门；origin/main=5493ff9。
- `but` 全量写操作；commit 用 `but status` 取文件 ID＋`but commit -b <branch> -m "..." <id>...`。
- `.code-tmp/r18-audit/*` 为前轮遗留未跟踪件（非本票勿动）；`.code-tmp/` 不入库。
- dist 已入库——后续改 src 必须同 commit 带 dist 重建（rebuild-diff CI 会拦忘重建）；`npm run build`=tsc+bundle 一条龙。
- store.ts 懒加载：新增 duckdb 调用面走同一 `loadDuckdb()` 门面，勿回退静态 import（G13 bundle 断言+clone 降级依赖它）。

## Relevant files

- `D:\Aworker\6F\engine\.mcp.json`、`engine\scripts\{gen-manifests.mjs,build-bundle.mjs}`、`engine\src\{fact\store.ts,demo\demo.ts}`
- `D:\Aworker\6F\engine\dist\`（40 件入库产物）、`.github\workflows\engine-ci.yml`、`engine\upstream-lock.yaml`
- `D:\Aworker\6F\.scratch\architecture-recovery\reports\{34-check.mjs,41b-check.mjs}`（已换约）
- `D:\Aworker\6F\.scratch\macro-audit\reports\2026-09-18-report.md`（本票证据总账）
- 任务书 `D:\Aworker\6F\.scratch\macro-audit\handoffs\next-round.md`（T2~T9 原文继续有效）
