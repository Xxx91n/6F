# Handoff — 轮 8 窗口：票 #35（A-040）CodeLore 首批 30 面契约化

## Status
DONE — #35 全完成定义闭环，A-040 已回写 done（2026-09-15）。分支 `r8-35-codelore-batch1`（commit `lro`，GitButler 叠于 r7 栈上，未 push——用户闸门）。

## Repo State
- `but status`：分支 `r8-35-codelore-batch1`（本票 1 commit）；下叠 r7-closeout-docs（pvy/xzx）。zz uncommitted 应为空——若有残留非本会话产物。
- engine：`npm test` 全绿（GEN-OK / SMOKE 6/6 / COLLECTORS 14/14 / ADAPTER 7/7 / BATCH1 41/41）；`npm run package` → macro-audit-0.1.0.tgz 31 files；`node dist/cli.js selftest` → ok:true。

## Completed（证据可复跑）
- `codelore analyze --help` 枚举存档 → `D:\Aworker\6F\.scratch\architecture-recovery\reports\35-analyze-help.txt`（57 面）
- 30 面对账零差异 → `35-facet-reconciliation.json`（含残余 4 面登记 + 速写映射）
- 适配器扩展：`D:\Aworker\6F\engine\src\upstream\codelore.ts`（CODELORE_BATCH1_FACETS / runCodeloreAnalysis / parseJsonRows / collectCodeloreFacets / pushResolutionFact）
- cassette 38 件 + manifest：`D:\Aworker\6F\engine\test\fixtures\codelore\batch1\`
- 契约测试：`D:\Aworker\6F\engine\test\codelore-batch1.test.mjs`（41 用例，入 smoke 链 → CI 三平台×node 20/22 同跑）
- 探针+守卫：`D:\Aworker\6F\.scratch\architecture-recovery\reports\35-probe.mjs` / `35-check.mjs`（PASS 21/21 exit 0）
- 调研：`35-atomcode-research.md`（code-maat/CodeScene/CodeLore 三先例；golden cassette 六步法）
- 报告：`35-report.md`（6 段式）+ `D:\Aworker\6F\.scratch\macro-audit\reports\2026-09-15-report.md`（窗口 2 节，追加式未覆盖窗口 1）

## Remaining / 交收口窗口裁决
- **枚举残余 4 面**未登记（entity-effort / architecture-violations / finding-hotspot-overlap / defect-validation）：在 `unregistered_residual`；若纳入暂缓面集须先补 D-035④ 账本原文（否则 33-check B3 FAIL）。
- README 61 面 vs 枚举 57 计数差已登记，契约锚 = 二进制枚举。

## Warnings
- 8 个空结果面（单人仓语义：stale-code/release-cadence/god-classes/dependency-cycles/knowledge-islands/communication/marginal-owner-risk/pair-programming）——列契约走 csv cassette 通道；接入多作者试点仓（#37）后这些面将产生真实行，cassette 需重录+漂移登记。
- `collectCodeloreFacets` 对大面（coordination-needs 363 行）fact 载荷 ~95KB——Macro-C 入库层（#38）若需分页/哈希化在那时定，适配层不预设。
- `.scratch/macro-audit/reports/{日期}-report.md` 是**按日期累计**的窗口报告——追加新窗口节，切勿整文件覆盖（本会话已踩过一次坑，已恢复）。

## Key Decisions
- 契约 = argv 全体：code-age 的 `--age-time-now` 与 messages 的 `-e` 冻结进 `extraArgs`（边界冻结不确定性，调研推荐做法）。
- collect 注入面（resolver/runner 可注入）使契约测试零二进制依赖 → CI 任意平台可跑。
- 适配层禁词表沿用 #31：threshold|verdict|RED|score_band。

## Read Next
1. `D:\Aworker\6F\.scratch\macro-audit\handoffs\next-round.md`（常驻任务书——#35 done 后解锁 W13：#36/#37/#41a）
2. `D:\Aworker\6F\.scratch\architecture-recovery\prompts\36-*.md`（若已立案；否则 issues/ 目录查 36）
3. `D:\Aworker\6F\engine\src\upstream\codelore.ts`（扩面模式样板）
4. `D:\Aworker\6F\.scratch\architecture-recovery\reports\35-check.mjs`（守卫断言面）
