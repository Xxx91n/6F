# 2026-09-18 报告 — 轮 20 T1 / BACKLOG #59：kernel 自包含分发修复（A-069 · commit lvr · 分支 r20-59-kernel-selfcontained）

## ① 完成定义对照（逐条·含可复跑证据）

任务书 T1 八子项 + 用户验收四件，逐条：

| # | 完成定义 | 状态 | 可复跑证据 |
|---|---|---|---|
| ① | `.mcp.json` = `command:"node"` + `args:["${CLAUDE_PLUGIN_ROOT}/dist/cli.js","mcp"]`（变量放 args 不放 command） | ✅ | `cd D:/Aworker/6F/engine && cat .mcp.json` → `"command": "node"` + args[0]=`${CLAUDE_PLUGIN_ROOT}/dist/cli.js` args[1]=`mcp`；生成器单源 `node scripts/gen-manifests.mjs` → GEN-OK 幂等 |
| ② | dist 入库；esbuild vs tsc-dist 由 duckdb native smoke 定形 | ✅ bundle 定形 | `ls engine/dist/` = 40 件（bundle cli.js 153,604B + tsc 树）；smoke：`node dist/cli.js --version/selftest/demo --list` 全通 + MCP handshake 实测（下详）；`engine/.gitignore` 已无 `dist/` 行 |
| ③ | CI rebuild-diff 守卫挂 34/41b 链 | ✅ | `.github/workflows/engine-ci.yml` build 后 `test -z "$(git status --porcelain -- dist/)"`（shell:bash，porcelain 捕未跟踪新文件）；34-check G13/G14 本地钉（`node .scratch/architecture-recovery/reports/34-check.mjs` → PASS 14/14） |
| ④ | README/SKILL 同票文档 | ✅ | `engine/README.md` §插件安装（Prerequisites Node≥20/`node --version` 自检＋安装两行＋/mcp 验收＋能力分级＋Windows bug 链 #43380/#65579＋`claude --debug` 引导）；`engine/skills/macro-audit/SKILL.md` §默认模式运行前提注记；根 `README.md` 状态注同步（过渡句式保 41a-C9 pinned 子串） |
| ⑤ | 守卫扩「.mcp.json command 禁裸名」 | ✅ | 34-check G12：`srv.command === 'node' || srv.command 含 ${CLAUDE_PLUGIN_ROOT}` → PASS；G9 换约钉 args 契约 |
| ⑥ | esbuild 入 upstream-lock | ✅ | `engine/upstream-lock.yaml` 末行 `id: esbuild / version: "0.28.2" / pin_type: exact-version / status: active`；`package.json` devDependencies `"esbuild": "0.28.2"` 精确值＋package-lock 同步；44-check PASS 56/56 |
| ⑦ | npm publish 维持 deferred | ✅ | 无发布动作；`files`/`publishConfig` 未动 |
| ⑧ | 真机 `/plugin install` 后 `/mcp` connected | ✅ 等价环实证 | `claude --plugin-dir D:/Aworker/6F/engine mcp list`（v2.1.251/Windows）→ `plugin:6f:macro-audit-kernel: node D:/Aworker/6F/engine/dist/cli.js mcp - ✔ Connected`——exec-form `${CLAUDE_PLUGIN_ROOT}` 展开实证免疫 hook 面 bug 链（#43380/#65579 如票注） |
| A | 编译通过 | ✅ | `npm run build` = `tsc -p tsconfig.json && node scripts/build-bundle.mjs` → BUNDLE-OK |
| B | 打包通过 | ✅ | `npm run package`（npm pack --dry-run）→ 71 files / 167.0kB / macro-audit-0.1.0.tgz |
| C | 启动并测活软件进程 | ✅ | `node dist/cli.js selftest` → `{"ok":true,checks:[...5/5 pass]}`；MCP 子进程握手：initialize → `{"protocolVersion":"2024-11-05","serverInfo":{"name":"macro-audit"}}` + tools/list → `facts` |
| D | 每平台 test 闭环 | ✅ 机制在＋本机实证 | engine-ci.yml matrix os×3(ubuntu/windows/macos)×node×2(20/22)：install→gen→build→rebuild-diff→package→smoke 全链每格跑；本机=Windows 全链绿；CI 远端绿随下次 push（闸门内） |

## ② 关键实现件

- `engine/scripts/build-bundle.mjs`（新）：esbuild buildSync `--packages=external`——唯一运行时 dep @duckdb/node-api 原生绑定不可内联保持外部解析；shebang 沿源。
- `engine/src/fact/store.ts`：duckdb 改懒加载（dynamic import + DuckdbError 统一降级）——git-clone 无 node_modules 时握手/selftest/repo-add/demo --list 零依赖可用，`facts`/`audit`/`demo` 跑批报 `DUCKDB-UNAVAILABLE` 结构化错（isError 非崩溃），消息内含恢复指引 `npm install`。
- `engine/src/demo/demo.ts`：`DEFINITIONS_DIR` 锚 `metaPath()`（引擎根）——bundle/tsc-dist 两形态 fixtures 同构可解析。
- `engine/scripts/gen-manifests.mjs`：`.mcp.json` 目标换约（command=node＋PLUGIN_ROOT args）；`mcp.json`（npm 消费者面）保留 bare `macro-audit` 不动——分发通道分工如 D-067。

## ③ 无 node_modules clone 仿真（重要降级证据）

插件目录全量拷贝仿真（`.code-tmp/r20-t1/plugin-sim`，已弃——零 node_modules 实测）：
```
node dist/cli.js --version  → {"name":"6f","version":"0.1.0",...} ✓
node dist/cli.js selftest   → ok:true 5/5 ✓
node dist/cli.js demo --list → 三场景列举 ✓（fixtures 锚修复生效）
node dist/cli.js mcp + initialize → 2024-11-05 握手 ✓
tools/call facts → isError "MCP-FACTS-ERROR: DUCKDB-UNAVAILABLE…npm install 后恢复" ✓ 结构化降级
```

## ④ 阻塞 / 残余缺口（如实登记）

1. **干净 git-clone 仍无 duckdb**：bundle 交付握手+只读壳零依赖可用，但 facts/audit/demo 跑批需插件目录 `npm install --omit=dev`（原生绑定不可 bundle）。候选补救=vendored bindings（仓重+供应链治理）／SessionStart bootstrap（同 #43380 bug 链未免疫）／duckdb-wasm 换绑（API 异构）——**另立票待裁**，本票以能力分级披露兜底不硬凑。
2. **真机验收=plugin-dir 环非 marketplace 重装**：D-067⑧ 字面验收（/plugin install→/mcp）需新 commit push 后重进市场缓存——push 停用户闸门未执行；`--plugin-dir` 等价覆盖宿主拉起链（变量展开+spawn+握手同一路径）。
3. **41a-check 陈旧 FAIL 4/39**：C10（与 41b-C1 互斥：41b 要求 capability 3 of 5 在 README 而 41a-C10 禁其出现——冻结期后 #41b 落地所致）＋D6/D7（CHANGELOG a_range/ledger_pointer 未随 A-068/D-070 进账）＋F4（next-round 已换代为轮 20 任务书）——全为 R19 收口件（commit xql）漂移归因非本票引入；冻结快照按 R14 lesson 不复写，移交 T8 值守清单。本票引入的 C9 已以过渡句式修复回绿。
4. **41a D6 现指 A-069**：账本新行后 D6 want 变为 `A-001 ~ A-069`——陈旧面扩大 1 项，同因移交。
5. **勘误（r20 审计 R1，2026-09-18 复审窗）**：初版报告/交接/A-069 行/WORKFLOW lessons 四处「dist 41 件」计数错误——`git ls-files engine/dist | wc -l` 实=40，已全部同源修正为 40。
6. **陈旧守卫总账补注（r20 审计 §5 追加扫面）**：43-check D5（钉 next-round T14 已换代）/45-check B5（`codelore:` 子串误吞，baeace8 基线已含）/50-check E2+E3（registry narrative-eval-surface=pending＋#52 已拆 #52a/#52b）/t8-check A3（同 registry 钉）——全先于本票存在、同 41a 族陈旧归因，移交 T8 值守清单一并复核。

## ⑤ lessons 候选（已落 WORKFLOW §4）

1. `${}` 模板插值陷阱=转义族第四变种（W2 `\`→W3 双解→R8 `\\`→本票 `${}`）：ctx_execute 写含 `${}` 文本必炸 ReferenceError——行数组 join 或 edit 工具规避。
2. 原生依赖不可内联时正确姿势=懒加载降级非硬打包；能力分级披露（零依赖面 vs npm install 面）胜过假自包含。
3. `claude --plugin-dir` + `claude mcp list` = 免 push 真机验收环（A-068 本地环回法第二型）。
4. rebuild-diff 用 `git status --porcelain` 非 `git diff --exit-code`——捕未跟踪新文件。
5. 冻结守卫钉住的文案修改=过渡句式兼容 pinned 子串；不可兼容者归因移交不静默。

## ⑥ 引用文件清单（commit lvr · 55 件）

- 行为：engine/.mcp.json、engine/.gitignore、engine/package.json、engine/package-lock.json、engine/scripts/{gen-manifests.mjs,build-bundle.mjs}、engine/src/{fact/store.ts,demo/demo.ts}
- dist 40 件：engine/dist/**（cli.js=bundle 153,604B + tsc 模块树+.d.ts）
- 守卫：.github/workflows/engine-ci.yml、.scratch/architecture-recovery/reports/{34-check.mjs,41b-check.mjs}
- 文档：README.md、engine/README.md、engine/skills/macro-audit/SKILL.md、engine/upstream-lock.yaml
- 账本文书（本 commit 外续随收口 commit）：BACKLOG.md #59 行✅、decision-ledger.md R11/A-069、WORKFLOW.md lessons 行、本报告

## ⑦ 守卫总账（收口时点全量复跑）

```
33-check PASS 16/16   34-check PASS 14/14   41a-check FAIL 4/39(陈旧归因)
41b-check PASS 30/30  42-check PASS 43/43   44-check PASS 56/56
46-check PASS 30/30   52a-check PASS 22/22  53-check PASS 25/25
54-check PASS 20/20   55-check PASS 18/18   npm test exit 0（15 smoke 套件全绿）
claude plugin validate engine --strict → Validation passed（D-066 advisory 面顺带实证）
```
